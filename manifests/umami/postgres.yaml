apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: umami-db
  namespace: umami
spec:
  instances: 2
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: umami
      owner: umami
      secret:
        name: umami-secrets
