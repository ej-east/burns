apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: traefik-internal
  labels:
    traefik-instance: internal
spec:
  defaultCertificate:
    secretName: wildcard-elijah-tls
